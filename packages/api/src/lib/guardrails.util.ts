export function withinMaxPositionPct(
    followerId: string,
    symbol: string,
    incomingPct: number
): Promise<boolean> {
    // TODO Epic K – for now always return true
    return Promise.resolve(true);
} 